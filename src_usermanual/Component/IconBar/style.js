import { colors } from "../../colors"


export const Iconbar_style = {
    textAlign: 'center',
    color: colors.light1,
    minHeight : '50%',
    backgroundColor: colors.dark2,
    border: `solid 3px ${colors.light1}`,
    borderBottom: `solid 0px`
  }

// styling for element buttons 
export  const button_style = {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    justifyContent: 'space-around',
    color : colors.black,
    fontSize: '0.7em',
    minWidth: '90px',
    fontWeight: 'bold',
    
  }

export const Popup_style = {
  
}